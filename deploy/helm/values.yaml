image:
  repository: ghcr.io/esatt10/pheasant
  tag: 0.10.0
  pullPolicy: IfNotPresent

replicaCount: 1

service:
  type: ClusterIP
  port: 8765

config:
  pheasantYaml: |
    pheasant:
      name: helm-pheasant
      environment: kubernetes
      state_path: /state
      workspace_root: /workspace
      exports_path: /exports
    server:
      host: 0.0.0.0
      port: 8765
      mcp:
        enabled: true
        transports:
          streamable_http: true
      api:
        enabled: true
    storage:
      sqlite_path: /state/pheasant.db
      graph_path: /state/graphs
      manifest_path: /state/manifests
    search:
      default_mode: hybrid
    sync:
      startup:
        full_validation: true
      scheduler:
        enabled: true
        interval_seconds: 900
    sources: []

persistence:
  state:
    enabled: true
    size: 10Gi

resources:
  requests:
    cpu: 500m
    memory: 1Gi
  limits:
    cpu: "2"
    memory: 6Gi

securityContext:
  runAsNonRoot: true
  runAsUser: 10001
  fsGroup: 10001

containerSecurityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop:
      - ALL
