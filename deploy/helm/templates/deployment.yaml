apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "syncsage.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "syncsage.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "syncsage.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "syncsage.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      securityContext:
{{ toYaml .Values.securityContext | indent 8 }}
      containers:
        - name: syncsage
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8765
          env:
            - name: SYNCSAGE_CONFIG
              value: /config/syncsage.yaml
          volumeMounts:
            - name: config
              mountPath: /config
              readOnly: true
            - name: state
              mountPath: /state
            - name: vault
              mountPath: /vault
            - name: workspace
              mountPath: /workspace
              readOnly: true
            - name: exports
              mountPath: /exports
          livenessProbe:
            httpGet:
              path: /health
              port: http
          readinessProbe:
            httpGet:
              path: /ready
              port: http
          resources:
{{ toYaml .Values.resources | indent 12 }}
          securityContext:
{{ toYaml .Values.containerSecurityContext | indent 12 }}
      volumes:
        - name: config
          configMap:
            name: {{ include "syncsage.fullname" . }}-config
        - name: state
          persistentVolumeClaim:
            claimName: {{ include "syncsage.fullname" . }}-state
        - name: vault
          persistentVolumeClaim:
            claimName: {{ include "syncsage.fullname" . }}-vault
        - name: workspace
          emptyDir: {}
        - name: exports
          emptyDir: {}
