apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "syncsage.fullname" . }}-config
  labels:
    app.kubernetes.io/name: {{ include "syncsage.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
data:
  syncsage.yaml: |
{{ .Values.config.syncsageYaml | indent 4 }}
