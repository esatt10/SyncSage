apiVersion: v1
kind: Service
metadata:
  name: pheasant
  namespace: pheasant
  labels:
    app.kubernetes.io/name: pheasant
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pheasant
  ports:
    - name: http
      port: 8765
      targetPort: http
